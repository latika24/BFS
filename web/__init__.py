"""
GigSure.com — the commercial website layer.

Everything in here is worker-facing marketing: what we sell, what it costs,
what happens when you claim, and why the policy belongs to the rider rather
than the app they ride for.

It is deliberately separated from `views/`, which is the product itself (the
rider app and the insurer console). The site sells; the app serves; the
console runs the book. Same engine, same store, three different audiences.
"""
